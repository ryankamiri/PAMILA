# OpenTripPlanner Operations Placeholder

This directory is reserved for the Map/Commute Agent.

Future work should add Dockerized OpenTripPlanner setup, local data download scripts, and notes for using NYC OpenStreetMap extracts with MTA static GTFS feeds. Keep downloaded routing data under `data/otp/` so it remains untracked.
